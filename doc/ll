stack.push(S,$)
input=read()

while(true){
sym=stack.peek()
if(sym.is(input)){
  write(input,sym)
  stack.pop()
  input=read()
}else{
  rule=getRule(input,sym)
  stack.pop()
  stack.push(rule.rhs)
}
}
